---
phase: 01-governance-safe-rollout
plan: 04
type: execute
wave: 3
depends_on:
  - 01-governance-safe-rollout-03
files_modified:
  - app/routes/__tests__/helpers/jsdom-bootstrap.ts
  - app/routes/__tests__/governance-mainflow.spec.ts
autonomous: true
requirements:
  - METR-02
gap_closure: true
must_haves:
  truths:
    - "治理主链路回归测试可在仓库默认 Node 测试模式下稳定执行，不再出现 document is not defined。"
    - "Settings > Features 的 LifeBegins UI 断言与 setter/toast 回归断言可自动化运行并通过。"
    - "执行 `pnpm test -- app/routes/__tests__/governance-mainflow.spec.ts` 时 4/4 用例通过。"
  artifacts:
    - path: "app/routes/__tests__/helpers/jsdom-bootstrap.ts"
      provides: "Node 测试下可复用的 JSDOM 全局注入与清理工具"
    - path: "app/routes/__tests__/governance-mainflow.spec.ts"
      provides: "主链路 + FeaturesTab UI 的同文件回归覆盖（服务端断言 + DOM 断言）"
  key_links:
    - from: "app/routes/__tests__/governance-mainflow.spec.ts"
      to: "app/routes/__tests__/helpers/jsdom-bootstrap.ts"
      via: "UI describe 的 beforeAll/afterAll DOM 安装与卸载"
      pattern: "installJSDOMGlobals|teardownJSDOMGlobals"
    - from: "app/routes/__tests__/governance-mainflow.spec.ts"
      to: "app/components/@settings/tabs/features/FeaturesTab.tsx"
      via: "React Testing Library render + switch click"
      pattern: "render\\(React\\.createElement\\(FeaturesTab\\)\\)"
---

<objective>
关闭 Phase 1 当前唯一阻塞验证 gap：修复 `governance-mainflow.spec.ts` 在 Node 环境执行 UI 断言时 `document is not defined` 的问题。

Purpose: 让 METR-02 的“治理开关可操作回归”在现有仓库测试配置下可执行、可复现、可持续。
Output: JSDOM bootstrap 测试工具 + 可稳定通过的 governance-mainflow 回归测试。
</objective>

<execution_context>
@/Users/zhouxiaopeng/.codex/get-shit-done/workflows/execute-plan.md
@/Users/zhouxiaopeng/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-governance-safe-rollout/01-VERIFICATION.md
@.planning/phases/01-governance-safe-rollout/01-CONTEXT.md
@.planning/phases/01-governance-safe-rollout/01-03-SUMMARY.md
@app/routes/__tests__/governance-mainflow.spec.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add deterministic JSDOM bootstrap helper for Node-mode governance tests</name>
  <files>app/routes/__tests__/helpers/jsdom-bootstrap.ts</files>
  <read_first>
    - Create: app/routes/__tests__/helpers/jsdom-bootstrap.ts.
    - Source of truth: .planning/phases/01-governance-safe-rollout/01-VERIFICATION.md gap reason/missing items.
    - Pattern reference: app/routes/__tests__/governance-mainflow.spec.ts current RTL render assertions.
  </read_first>
  <behavior>
    - Test utility can install DOM globals before UI assertions and fully restore globals after assertions.
    - Utility works in Vitest Node environment without requiring global `@vitest-environment jsdom`.
  </behavior>
  <action>创建 `jsdom-bootstrap.ts`，导出 `installJSDOMGlobals()` 与 `teardownJSDOMGlobals()`（或等价命名）并使用 `new JSDOM('<!doctype html><html><body></body></html>', { url: 'http://localhost/' })`。安装时显式挂载这些全局：`window`、`document`、`navigator`、`HTMLElement`、`Node`、`MutationObserver`、`getComputedStyle`；卸载时恢复原值并关闭 `window`。此工具仅服务测试，不改生产代码。</action>
  <acceptance_criteria>
    - `rg -n "installJSDOMGlobals|teardownJSDOMGlobals" app/routes/__tests__/helpers/jsdom-bootstrap.ts` 命中导出函数。
    - `rg -n "new JSDOM\\('<!doctype html><html><body></body></html>'" app/routes/__tests__/helpers/jsdom-bootstrap.ts` 命中。
    - `rg -n "window|document|navigator|HTMLElement|Node|MutationObserver|getComputedStyle" app/routes/__tests__/helpers/jsdom-bootstrap.ts` 命中上述全局挂载逻辑。
  </acceptance_criteria>
  <verify>
    <automated>pnpm test -- app/routes/__tests__/governance-mainflow.spec.ts</automated>
  </verify>
  <done>仓库内存在可复用的 Node/JSDOM 引导工具，可供治理回归用例稳定挂载 DOM。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire governance-mainflow UI assertions to JSDOM bootstrap and keep setter/toast coverage</name>
  <files>app/routes/__tests__/governance-mainflow.spec.ts, app/routes/__tests__/helpers/jsdom-bootstrap.ts</files>
  <read_first>
    - Modify: app/routes/__tests__/governance-mainflow.spec.ts.
    - Read: app/routes/__tests__/helpers/jsdom-bootstrap.ts.
    - Source of truth: .planning/phases/01-governance-safe-rollout/01-VERIFICATION.md missing items.
    - Decision constraints: .planning/phases/01-governance-safe-rollout/01-CONTEXT.md D-04（默认全关） and D-07（setter+toast 反馈保持一致）。
  </read_first>
  <behavior>
    - Test 1: server-only governance assertions still run in Node mode.
    - Test 2: UI visibility assertions render FeaturesTab with installed DOM and validate 5 switches visible under all-false flags (per D-04).
    - Test 3: UI toggle assertion still verifies existing setter + toast success contract (per D-07).
  </behavior>
  <action>在 `governance-mainflow.spec.ts` 的 `governance features tab visibility` describe 中接入 Task 1 工具：`beforeAll` 安装 DOM，`afterAll` 卸载并恢复全局；保留现有两条 UI 断言并确保关键断言文本不变：`setLifeBeginsAnchorEnabled` 被调用 `true`，`toast.success` 文案为 `lifebegins.anchor enabled`。不要将整文件切到 jsdom 环境，保持服务端断言仍走 Node 运行。</action>
  <acceptance_criteria>
    - `rg -n "beforeAll\\(|afterAll\\(|installJSDOMGlobals|teardownJSDOMGlobals" app/routes/__tests__/governance-mainflow.spec.ts` 命中。
    - `rg -n "setLifeBeginsAnchorEnabled\\)\\.toHaveBeenCalledWith\\(true\\)" app/routes/__tests__/governance-mainflow.spec.ts` 命中。
    - `rg -n "lifebegins\\.anchor enabled" app/routes/__tests__/governance-mainflow.spec.ts` 命中。
    - `pnpm test -- app/routes/__tests__/governance-mainflow.spec.ts` 输出 4 passed, 0 failed。
  </acceptance_criteria>
  <verify>
    <automated>pnpm test -- app/routes/__tests__/governance-mainflow.spec.ts</automated>
  </verify>
  <done>governance-mainflow 回归在默认 Node 测试链路下可运行并通过，且 UI setter/toast 行为得到自动化覆盖。</done>
</task>

</tasks>

<verification>
执行 `pnpm test -- app/routes/__tests__/governance-mainflow.spec.ts`，确认无 `document is not defined` 且 4 个用例全部通过；再执行 `pnpm test -- app/components/@settings/tabs/features/lifebeginsFeatures.spec.ts`，确认五开关可见性契约未回归。
</verification>

<success_criteria>
Phase 1 验证 blocker 被关闭：治理主链路回归测试在仓库默认测试配置下稳定通过，METR-02 的开关可操作回归具备自动化证据闭环。
</success_criteria>

<output>
After completion, create `.planning/phases/01-governance-safe-rollout/01-governance-safe-rollout-04-SUMMARY.md`
</output>
