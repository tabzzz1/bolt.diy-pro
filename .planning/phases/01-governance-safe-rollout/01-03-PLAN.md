---
phase: 01-governance-safe-rollout
plan: 03
type: execute
wave: 2
depends_on:
  - 01-governance-safe-rollout-01
files_modified:
  - app/components/@settings/tabs/features/FeaturesTab.tsx
  - app/components/@settings/tabs/features/lifebeginsFeatures.ts
  - app/components/@settings/tabs/features/lifebeginsFeatures.spec.ts
autonomous: true
requirements:
  - METR-02
gap_closure: true
must_haves:
  truths:
    - "Settings > Features 的 LifeBegins 区域在默认全关时仍显示 5 个开关。"
    - "开关的 on/off 仅影响 Switch checked 状态，不再影响开关卡片是否渲染。"
    - "切换任一 lifebegins 开关仍能走既有 setter 与 toast 反馈，不破坏其他设置项。"
  artifacts:
    - path: "app/components/@settings/tabs/features/FeaturesTab.tsx"
      provides: "LifeBegins 开关渲染逻辑（移除按 enabled 的可见性过滤）"
    - path: "app/components/@settings/tabs/features/lifebeginsFeatures.ts"
      provides: "可测试的 LifeBegins 开关列表构造契约"
    - path: "app/components/@settings/tabs/features/lifebeginsFeatures.spec.ts"
      provides: "默认全关仍显示五个开关的回归测试"
  key_links:
    - from: "app/components/@settings/tabs/features/FeaturesTab.tsx"
      to: "app/lib/hooks/useSettings.ts"
      via: "lifebegins.* enabled flags + setLifeBegins* setters"
      pattern: "lifebegins(Anchor|Fork|Failure|Timeline|Dna)Enabled|setLifeBegins"
    - from: "app/components/@settings/tabs/features/FeaturesTab.tsx"
      to: "app/components/@settings/tabs/features/lifebeginsFeatures.ts"
      via: "buildLifeBeginsFeatures"
      pattern: "buildLifeBeginsFeatures"
---

<objective>
关闭 UAT Test 6 的可见性回归：修复 FeaturesTab 中 LifeBegins 开关“默认全关即全隐藏”的错误行为，并补齐自动化回归测试。

Purpose: 恢复 METR-02 的治理可操作性，确保运营在默认全关状态下仍能看到并切换五个域开关。
Output: FeaturesTab 渲染修复、可复用开关构造函数、可自动回归的测试。
</objective>

<execution_context>
@/Users/zhouxiaopeng/.codex/get-shit-done/workflows/execute-plan.md
@/Users/zhouxiaopeng/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-governance-safe-rollout/01-UAT.md
@.planning/phases/01-governance-safe-rollout/01-CONTEXT.md
@.planning/phases/01-governance-safe-rollout/01-01-SUMMARY.md
@app/components/@settings/tabs/features/FeaturesTab.tsx
@app/lib/hooks/useSettings.ts

<interfaces>
From app/lib/hooks/useSettings.ts:
```typescript
export interface UseSettingsReturn {
  lifebeginsAnchorEnabled: boolean;
  lifebeginsForkEnabled: boolean;
  lifebeginsFailureEnabled: boolean;
  lifebeginsTimelineEnabled: boolean;
  lifebeginsDnaEnabled: boolean;
  setLifeBeginsAnchorEnabled: (enabled: boolean) => void;
  setLifeBeginsForkEnabled: (enabled: boolean) => void;
  setLifeBeginsFailureEnabled: (enabled: boolean) => void;
  setLifeBeginsTimelineEnabled: (enabled: boolean) => void;
  setLifeBeginsDnaEnabled: (enabled: boolean) => void;
}
```

From app/components/@settings/tabs/features/FeaturesTab.tsx:
```typescript
interface FeatureToggle {
  id: string;
  title: string;
  description: string;
  icon: string;
  enabled: boolean;
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add visibility-regression contract for LifeBegins switch list</name>
  <files>app/components/@settings/tabs/features/lifebeginsFeatures.ts, app/components/@settings/tabs/features/lifebeginsFeatures.spec.ts</files>
  <read_first>
    - Modify: app/components/@settings/tabs/features/lifebeginsFeatures.ts and app/components/@settings/tabs/features/lifebeginsFeatures.spec.ts.
    - Source of truth: .planning/phases/01-governance-safe-rollout/01-UAT.md (Test 6 gap + root_cause), .planning/phases/01-governance-safe-rollout/01-VERIFICATION.md (failed truth + missing items), .planning/phases/01-governance-safe-rollout/01-CONTEXT.md D-04.
    - Reference target integration file: app/components/@settings/tabs/features/FeaturesTab.tsx (helper consumption contract).
  </read_first>
  <behavior>
    - Test 1: all five lifebegins flags are false -> returned list length remains 5 (per D-04, fixes UAT gap #6).
    - Test 2: each returned item keeps its domain id and enabled boolean state (enabled only controls checked state).
    - Test 3: mixed true/false inputs still return all 5 items without filtering by enabled.
  </behavior>
  <action>创建 `buildLifeBeginsFeatures`（或等效命名）纯函数，输入五个 lifebegins 布尔值，稳定输出五个 `FeatureToggle` 项；在 `.spec.ts` 中先写失败用例覆盖“默认全关仍显示五项”契约，再实现函数使测试通过。此任务只建立可见性契约，不改 UI 事件处理逻辑。</action>
  <acceptance_criteria>
    - `rg -n "export\\s+(const|function)\\s+buildLifeBeginsFeatures" app/components/@settings/tabs/features/lifebeginsFeatures.ts` 命中导出构造函数。
    - `rg -n "length\\)\\.toBe\\(5\\)|toHaveLength\\(5\\)|without filtering by enabled" app/components/@settings/tabs/features/lifebeginsFeatures.spec.ts` 命中“全关仍为 5 项”契约断言。
    - `pnpm test -- app/components/@settings/tabs/features/lifebeginsFeatures.spec.ts` 通过，且无 failed 用例。
  </acceptance_criteria>
  <verify>
    <automated>pnpm test -- app/components/@settings/tabs/features/lifebeginsFeatures.spec.ts</automated>
  </verify>
  <done>存在独立、可测试的 LifeBegins 列表构造逻辑，且测试覆盖“不可按 enabled 过滤”的核心回归点。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Refactor FeaturesTab to render all LifeBegins switches regardless of enabled state</name>
  <files>app/components/@settings/tabs/features/FeaturesTab.tsx, app/components/@settings/tabs/features/lifebeginsFeatures.ts, app/components/@settings/tabs/features/lifebeginsFeatures.spec.ts</files>
  <read_first>
    - Modify: app/components/@settings/tabs/features/FeaturesTab.tsx.
    - Read: app/components/@settings/tabs/features/lifebeginsFeatures.ts and app/lib/hooks/useSettings.ts.
    - Source of truth: .planning/phases/01-governance-safe-rollout/01-UAT.md (missing #1/#2), .planning/phases/01-governance-safe-rollout/01-VERIFICATION.md (failed truth + gap reason), .planning/phases/01-governance-safe-rollout/01-CONTEXT.md D-04 and D-07.
  </read_first>
  <behavior>
    - Test 1: FeaturesTab consumes the helper list and no longer applies `.filter((feature) => feature.enabled)`.
    - Test 2: LifeBegins section renders five switch cards when all lifebegins flags are false.
    - Test 3: toggling any card still calls existing `setLifeBegins*` setter and shows success toast.
  </behavior>
  <action>在 `FeaturesTab` 中移除当前对 lifebegins 列表的 enabled 过滤（对应 UAT gap missing #1/#2），改为直接使用 Task 1 构造函数输出数组；保持 `Switch.checked={feature.enabled}` 不变，仅将 enabled 作为状态值而非可见性条件。说明：D-05 继续用于“业务入口隐藏”，本修复仅针对“治理控制面板中的开关可操作性”。</action>
  <acceptance_criteria>
    - `rg -n "buildLifeBeginsFeatures" app/components/@settings/tabs/features/FeaturesTab.tsx` 命中 helper 使用。
    - `rg -n "filter\\(\\(feature\\) => feature\\.enabled\\)" app/components/@settings/tabs/features/FeaturesTab.tsx` 无命中（命令退出码应为 1）。
    - `pnpm test -- app/components/@settings/tabs/features/lifebeginsFeatures.spec.ts app/routes/__tests__/governance-mainflow.spec.ts` 通过，且无 failed 用例。
  </acceptance_criteria>
  <verify>
    <automated>pnpm test -- app/components/@settings/tabs/features/lifebeginsFeatures.spec.ts app/routes/__tests__/governance-mainflow.spec.ts</automated>
  </verify>
  <done>LifeBegins 区域在默认全关时不再空白，五个开关可见且可切换，原有治理主链路回归测试仍通过。</done>
</task>

</tasks>

<verification>
执行 `pnpm test -- app/components/@settings/tabs/features/lifebeginsFeatures.spec.ts app/routes/__tests__/governance-mainflow.spec.ts`，并确认 `FeaturesTab.tsx` 不再包含 `.filter((feature) => feature.enabled)`。
</verification>

<success_criteria>
UAT Test 6 由 issue 变为 pass：Settings > Features 页面固定展示五个 LifeBegins 开关，默认全关时仍可操作，且不影响其他设置行为。
</success_criteria>

<output>
After completion, create `.planning/phases/01-governance-safe-rollout/01-governance-safe-rollout-03-SUMMARY.md`
</output>
