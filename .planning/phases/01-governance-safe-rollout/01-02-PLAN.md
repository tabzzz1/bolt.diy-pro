---
phase: 01-governance-safe-rollout
plan: 02
type: execute
wave: 2
depends_on:
  - 01-governance-safe-rollout-01
files_modified:
  - app/lib/governance/growthDataRights.server.ts
  - app/lib/governance/audit.server.ts
  - app/lib/governance/__tests__/growth-export.spec.ts
  - app/routes/api.growth.export.ts
  - app/routes/api.growth.delete.ts
  - app/routes/__tests__/api.growth.delete.spec.ts
  - app/components/@settings/tabs/data/DataTab.tsx
  - app/lib/hooks/useDataOperations.ts
autonomous: true
requirements:
  - METR-03
must_haves:
  truths:
    - "用户可导出 growth 域数据，导出格式为单个 JSON 文件。"
    - "用户可同步删除 growth 域数据，删除后不保留业务可恢复副本。"
    - "删除流程仅记录最小匿名审计元数据（时间、动作、结果、域），不记录业务内容。"
  artifacts:
    - path: "app/routes/api.growth.export.ts"
      provides: "growth-only JSON export API"
    - path: "app/routes/api.growth.delete.ts"
      provides: "synchronous hard delete API with minimal audit"
    - path: "app/lib/governance/growthDataRights.server.ts"
      provides: "growth namespace allowlist serializer/deleter"
    - path: "app/routes/__tests__/api.growth.delete.spec.ts"
      provides: "METR-03 delete + audit contract regression"
  key_links:
    - from: "app/routes/api.growth.export.ts"
      to: "app/lib/governance/growthDataRights.server.ts"
      via: "exportGrowthDomainData"
      pattern: "exportGrowthDomainData"
    - from: "app/routes/api.growth.delete.ts"
      to: "app/lib/governance/audit.server.ts"
      via: "appendGovernanceAuditEvent"
      pattern: "appendGovernanceAuditEvent"
    - from: "app/components/@settings/tabs/data/DataTab.tsx"
      to: "app/routes/api.growth.export.ts + api.growth.delete.ts"
      via: "useDataOperations growth actions"
      pattern: "api/growth/(export|delete)"
---

<objective>
交付 Phase 1 数据权利最小闭环：growth 域独立导出/删除、同步硬删除、最小匿名审计。

Purpose: 满足 METR-03，建立不影响 legacy chat/settings 的数据治理边界。
Output: growth 数据权利服务、export/delete API、设置页入口接入、自动化验证。
</objective>

<execution_context>
@/Users/zhouxiaopeng/.codex/get-shit-done/workflows/execute-plan.md
@/Users/zhouxiaopeng/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-governance-safe-rollout/01-CONTEXT.md
@.planning/phases/01-governance-safe-rollout/01-RESEARCH.md
@app/components/@settings/tabs/data/DataTab.tsx
@app/lib/hooks/useDataOperations.ts
@app/lib/services/importExportService.ts
@app/lib/security.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build growth-domain data rights service with strict scope allowlist</name>
  <files>app/lib/governance/growthDataRights.server.ts, app/lib/governance/audit.server.ts, app/lib/governance/__tests__/growth-export.spec.ts</files>
  <read_first>
    - Modify: growth data-rights service and its tests.
    - Source of truth: 01-CONTEXT.md D-09 D-10 D-11 D-12; 01-RESEARCH.md "Growth-Scoped Data Rights".
  </read_first>
  <behavior>
    - Test 1: export payload contains only growth namespace data and schema marker `lifebegins.growth.v1`.
    - Test 2: export output is one JSON object file payload (D-10).
    - Test 3: delete operation removes growth records only and returns synchronous result summary (D-11, D-12).
  </behavior>
  <action>实现 `growthDataRights.server.ts`：固定 allowlist 仅处理 growth 域实体（明确排除 chats/settings/providers）；导出对象固定 `{ schema: "lifebegins.growth.v1", exportDate, data }`（per D-10）；删除采用同步硬删除并返回 `{ deletedCount, durationMs, result: "success"|"partial"|"failed" }`（per D-11 D-12）。在 `audit.server.ts` 实现最小匿名审计结构：`timestamp`, `action`, `result`, `featureDomain`，禁止写入业务内容（per D-12）。</action>
  <acceptance_criteria>
    - `rg -n "schema:\\s*\"lifebegins\\.growth\\.v1\"" app/lib/governance/growthDataRights.server.ts` 命中。
    - `rg -n "(timestamp|action|result|featureDomain)" app/lib/governance/audit.server.ts` 全部命中。
    - `pnpm test -- app/lib/governance/__tests__/growth-export.spec.ts` 通过并包含“exclude chats/settings”断言。
  </acceptance_criteria>
  <verify>
    <automated>pnpm test -- app/lib/governance/__tests__/growth-export.spec.ts</automated>
  </verify>
  <done>growth 数据导出/删除逻辑具备清晰边界，且具备最小匿名审计能力。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement growth export/delete routes with synchronous hard-delete contract</name>
  <files>app/routes/api.growth.export.ts, app/routes/api.growth.delete.ts, app/routes/__tests__/api.growth.delete.spec.ts</files>
  <read_first>
    - Modify: growth export/delete API routes and delete route tests.
    - Source of truth: 01-CONTEXT.md D-09 D-10 D-11 D-12; app/lib/security.ts withSecurity pattern.
  </read_first>
  <behavior>
    - Test 1: export route returns JSON response for growth scope only.
    - Test 2: delete route performs synchronous hard delete and returns completed result in same request.
    - Test 3: delete route writes anonymous audit metadata only (no business payload).
  </behavior>
  <action>新增 `api.growth.export.ts` 与 `api.growth.delete.ts`，均使用 `withSecurity` 包装并开启 `allowedMethods`（export=`GET`，delete=`POST`）；调用 Task 1 的服务方法；delete route 必须在单次请求内完成删除并返回结果，不引入异步队列（per D-12）；失败时返回结构化错误 JSON，status 使用 500/400，不吞错。</action>
  <acceptance_criteria>
    - `rg -n "allowedMethods: \\[\"GET\"\\]" app/routes/api.growth.export.ts` 命中。
    - `rg -n "allowedMethods: \\[\"POST\"\\]" app/routes/api.growth.delete.ts` 命中。
    - `pnpm test -- app/routes/__tests__/api.growth.delete.spec.ts` 通过并断言同步完成与匿名审计字段。
  </acceptance_criteria>
  <verify>
    <automated>pnpm test -- app/routes/__tests__/api.growth.delete.spec.ts</automated>
  </verify>
  <done>growth 导出/删除 API 可直接用于设置页，且行为满足最小数据权利边界。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Wire DataTab and useDataOperations to growth-specific export/delete actions</name>
  <files>app/components/@settings/tabs/data/DataTab.tsx, app/lib/hooks/useDataOperations.ts</files>
  <read_first>
    - Modify: data settings UI and data operation hook.
    - Source of truth: 01-CONTEXT.md D-09 D-10 D-11; DataTab/useDataOperations existing interaction model.
  </read_first>
  <behavior>
    - Test 1: DataTab can trigger growth-only export and download one JSON file.
    - Test 2: DataTab delete action calls growth delete API and renders synchronous success/failure feedback.
    - Test 3: legacy broad export/delete actions are not reused for growth rights boundary flow.
  </behavior>
  <action>在 `useDataOperations.ts` 新增 `handleExportGrowthData` 与 `handleDeleteGrowthData`，分别调用 `/api/growth/export` 和 `/api/growth/delete`；在 `DataTab.tsx` 增加独立入口区块 `Growth Data Rights`，按钮文案固定为 `Export Growth Data (JSON)` 与 `Delete Growth Data`；导出下载文件名固定 `lifebegins-growth-data.json`（per D-10）；删除完成后 toast 显示删除计数与耗时（per D-12）。</action>
  <acceptance_criteria>
    - `rg -n "/api/growth/(export|delete)" app/lib/hooks/useDataOperations.ts app/components/@settings/tabs/data/DataTab.tsx` 命中。
    - `rg -n "lifebegins-growth-data\\.json" app/lib/hooks/useDataOperations.ts` 命中。
    - `pnpm test -- app/routes/__tests__/api.growth.delete.spec.ts app/lib/governance/__tests__/growth-export.spec.ts` 通过。
  </acceptance_criteria>
  <verify>
    <automated>pnpm test -- app/routes/__tests__/api.growth.delete.spec.ts app/lib/governance/__tests__/growth-export.spec.ts</automated>
  </verify>
  <done>用户可在设置页直接执行 growth 域导出/删除，且边界不污染 legacy 数据域。</done>
</task>

</tasks>

<verification>
执行 `pnpm test -- app/lib/governance/__tests__/growth-export.spec.ts app/routes/__tests__/api.growth.delete.spec.ts`，确认 growth-only 导出、同步硬删除、最小匿名审计三个核心行为成立。
</verification>

<success_criteria>
满足 METR-03：新增 growth 域数据具备可导出与可删除边界，导出为单 JSON，删除为同步硬删除，审计仅保留匿名最小元数据。
</success_criteria>

<output>
After completion, create `.planning/phases/01-governance-safe-rollout/01-governance-safe-rollout-02-SUMMARY.md`
</output>
