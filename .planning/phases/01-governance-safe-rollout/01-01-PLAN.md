---
phase: 01-governance-safe-rollout
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/lib/governance/types.ts
  - app/lib/governance/errors.ts
  - app/lib/governance/featureFlags.server.ts
  - app/routes/api.governance.flags.ts
  - app/routes/__tests__/api.governance.flags.spec.ts
  - app/components/@settings/tabs/features/FeaturesTab.tsx
  - app/lib/hooks/useSettings.ts
  - app/lib/stores/settings.ts
  - app/routes/__tests__/governance-mainflow.spec.ts
autonomous: true
requirements:
  - METR-02
must_haves:
  truths:
    - "运营可按能力域独立启停 lifebegins.anchor/fork/failure/timeline/dna，并默认全关。"
    - "服务端在功能关闭时返回一致的 feature_disabled 拒绝语义，不静默放行。"
    - "即使所有 growth 开关关闭，原有聊天-工作台主链路仍可正常使用。"
  artifacts:
    - path: "app/lib/governance/featureFlags.server.ts"
      provides: "env > persisted > default 的服务端权威开关解析"
    - path: "app/routes/api.governance.flags.ts"
      provides: "按能力域读取/更新持久化配置的治理 API"
    - path: "app/components/@settings/tabs/features/FeaturesTab.tsx"
      provides: "前端可见性开关控制与关闭态隐藏入口"
    - path: "app/routes/__tests__/api.governance.flags.spec.ts"
      provides: "METR-02 自动化回归验证"
  key_links:
    - from: "app/routes/api.governance.flags.ts"
      to: "app/lib/governance/featureFlags.server.ts"
      via: "resolveGrowthFeatureFlags/assertGrowthFeatureEnabled"
      pattern: "resolveGrowthFeatureFlags|assertGrowthFeatureEnabled"
    - from: "app/components/@settings/tabs/features/FeaturesTab.tsx"
      to: "app/lib/hooks/useSettings.ts"
      via: "LifeBegins capability switches state binding"
      pattern: "lifebegins\\.(anchor|fork|failure|timeline|dna)"
---

<objective>
交付 Phase 1 的治理开关主干：服务端权威开关解析、统一拒绝语义、前端可见性开关层，并验证“全关不破主链路”。

Purpose: 满足 METR-02，确保新能力可灰度开启并可立即回滚。
Output: 可复用治理模块、治理 API、设置页开关接入、回归测试。
</objective>

<execution_context>
@/Users/zhouxiaopeng/.codex/get-shit-done/workflows/execute-plan.md
@/Users/zhouxiaopeng/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-governance-safe-rollout/01-CONTEXT.md
@.planning/phases/01-governance-safe-rollout/01-RESEARCH.md
@app/lib/security.ts
@app/components/@settings/tabs/features/FeaturesTab.tsx
@app/lib/hooks/useSettings.ts
@app/lib/stores/settings.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build server-authoritative growth feature flag resolver and contracts</name>
  <files>app/lib/governance/types.ts, app/lib/governance/errors.ts, app/lib/governance/featureFlags.server.ts, app/routes/__tests__/api.governance.flags.spec.ts</files>
  <read_first>
    - Modify: app/lib/governance/featureFlags.server.ts and governance contract files.
    - Source of truth: 01-CONTEXT.md D-01 D-02 D-03 D-04 D-06 D-08; 01-RESEARCH.md "Primary recommendation"; app/lib/security.ts error/response style.
  </read_first>
  <behavior>
    - Test 1: `resolveGrowthFeatureFlags` returns all domains false when no env/persisted config (D-04).
    - Test 2: env override beats persisted config and default (D-03).
    - Test 3: disabled domain is rejected with status 403 and `{ error: "feature_disabled", feature }` (D-06).
  </behavior>
  <action>新增 `GrowthFeatureDomain` 精确枚举（`lifebegins.anchor|fork|failure|timeline|dna`，per D-02）；在 `featureFlags.server.ts` 实现固定优先级 `env > persisted > defaults`（per D-03），默认值全 `false`（per D-04）；实现 `assertGrowthFeatureEnabled(domain, flags)`，关闭时返回固定 payload：`status=403`、`error=feature_disabled`、`feature=<domain>`、`message=Feature is disabled by governance policy`（per D-06）。禁止在路由中散落 ad-hoc 判断，统一走该模块。</action>
  <acceptance_criteria>
    - `rg -n "lifebegins\\.(anchor|fork|failure|timeline|dna)" app/lib/governance/featureFlags.server.ts` 至少命中 5 个域键。
    - `rg -n "env.*persisted.*default" app/lib/governance/featureFlags.server.ts` 命中优先级实现注释或逻辑。
    - `pnpm test -- app/routes/__tests__/api.governance.flags.spec.ts` 通过，且包含 403 + feature_disabled 断言。
  </acceptance_criteria>
  <verify>
    <automated>pnpm test -- app/routes/__tests__/api.governance.flags.spec.ts</automated>
  </verify>
  <done>服务端存在唯一权威开关解析器，具备分域、默认全关、环境变量硬切和统一拒绝语义。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement governance flags route with withSecurity wrapper</name>
  <files>app/routes/api.governance.flags.ts, app/routes/__tests__/api.governance.flags.spec.ts</files>
  <read_first>
    - Modify: app/routes/api.governance.flags.ts and route test.
    - Source of truth: 01-CONTEXT.md D-01 D-02 D-03 D-04; app/lib/security.ts withSecurity contract.
  </read_first>
  <behavior>
    - Test 1: GET returns all five growth domains with resolved effective values.
    - Test 2: POST/PATCH updates persisted flag config but env override remains dominant.
    - Test 3: non-allowed methods return 405 through security wrapper.
  </behavior>
  <action>新增 `api.governance.flags.ts`：`GET` 返回 `{ defaults, persisted, effective, precedence: "env>persisted>default" }`；`PATCH` 仅允许更新白名单五个域键布尔值；统一使用 `withSecurity(..., { allowedMethods: ["GET","PATCH"], rateLimit: true })`。持久化结构固定为 `growthFeatureFlags` 对象，禁止接受额外键，非法键返回 400。</action>
  <acceptance_criteria>
    - `rg -n "allowedMethods: \\[\"GET\", \"PATCH\"\\]" app/routes/api.governance.flags.ts` 命中。
    - `rg -n "precedence: \"env>persisted>default\"" app/routes/api.governance.flags.ts` 命中。
    - `pnpm test -- app/routes/__tests__/api.governance.flags.spec.ts` 通过并覆盖 GET/PATCH/405/400。
  </acceptance_criteria>
  <verify>
    <automated>pnpm test -- app/routes/__tests__/api.governance.flags.spec.ts</automated>
  </verify>
  <done>治理 API 可读写分域开关，且行为与安全包装符合既有 API 规范。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Wire front-end visibility switches and regression for main flow safety</name>
  <files>app/components/@settings/tabs/features/FeaturesTab.tsx, app/lib/hooks/useSettings.ts, app/lib/stores/settings.ts, app/routes/__tests__/governance-mainflow.spec.ts</files>
  <read_first>
    - Modify: FeaturesTab/useSettings/settings store and main-flow regression test.
    - Source of truth: 01-CONTEXT.md D-05 D-07 D-08; ROADMAP Phase 1 success criterion #2.
  </read_first>
  <behavior>
    - Test 1: all growth switches off hides growth entry points (not read-only placeholders) (D-05).
    - Test 2: when API returns feature_disabled, UI shows lightweight toast and keeps input state (D-07).
    - Test 3: all growth disabled still keeps chat-workbench path usable.
  </behavior>
  <action>在设置层增加 `lifebegins.*` 五个前端可见性开关，并在 `FeaturesTab` 增加对应卡片分组；关闭状态直接隐藏入口（per D-05）；收到 `feature_disabled` 时统一 toast 文案 `This feature is temporarily disabled.` 并跳回主链路（per D-07），不清空当前输入状态；不实现会话豁免缓存（per D-08）。新增 `governance-mainflow.spec.ts` 验证“全关不破主链路”。</action>
  <acceptance_criteria>
    - `rg -n "lifebegins\\.(anchor|fork|failure|timeline|dna)" app/components/@settings/tabs/features/FeaturesTab.tsx app/lib/hooks/useSettings.ts app/lib/stores/settings.ts` 命中。
    - `rg -n "temporarily disabled" app/components/@settings/tabs/features/FeaturesTab.tsx` 命中轻提示文案。
    - `pnpm test -- app/routes/__tests__/governance-mainflow.spec.ts` 通过。
  </acceptance_criteria>
  <verify>
    <automated>pnpm test -- app/routes/__tests__/governance-mainflow.spec.ts</automated>
  </verify>
  <done>前端可见性与服务端治理语义一致，且关闭所有 growth 能力时主链路不受损。</done>
</task>

</tasks>

<verification>
执行 `pnpm test -- app/routes/__tests__/api.governance.flags.spec.ts app/routes/__tests__/governance-mainflow.spec.ts`，确认 METR-02 的服务端拒绝语义、开关优先级和主链路安全回归全部通过。
</verification>

<success_criteria>
满足 METR-02：分能力域双层开关可灰度启停、可环境变量硬切回滚、关闭态不破坏既有聊天-工作台主链路。
</success_criteria>

<output>
After completion, create `.planning/phases/01-governance-safe-rollout/01-governance-safe-rollout-01-SUMMARY.md`
</output>
